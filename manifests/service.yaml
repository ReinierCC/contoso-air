apiVersion: v1
kind: Service
metadata:
  name: testflow
  namespace: namespace-workflow-1736798693789
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: testflow
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000